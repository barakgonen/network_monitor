/**
 * Field-level annotations that describe binary wire layout for classes decoded/encoded via
 * {@code com.example.schemacore.reflect.StructSizeCalculator} and
 * {@code com.example.schemacore.reflect.ReflectiveStructCodec}: array lengths and enum wire sizes,
 * neither of which are otherwise derivable by reflection alone.
 */
package com.example.schemacore.annotation;
